import React from 'react';

const Redirect = (props) => {

    props.history.push('/home');


    return (
        <div>
            <h2></h2>
            
        </div>
    );
}

export default Redirect;
